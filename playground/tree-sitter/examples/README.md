# Tree-sitter examples

Use `tree-sitter.yml` as the source of truth for the grammar, fixture, and query pairings. Keep experiments small and record only reproducible commands or observations here.

Example pairings:

```text
fixtures/javascript/basic.js -> queries/javascript/functions.scm
fixtures/typescript/basic.ts -> queries/typescript/types.scm
```
