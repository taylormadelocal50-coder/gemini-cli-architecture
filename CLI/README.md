# CLI Architecture

This directory contains the conceptual layers of the CLI architecture.

- [`SQL/`](SQL/) describes the domain data layer and its common relational machinery.
- [`architecture/`](architecture/) contains the MESH state model and Tree-sitter playground.

The current conceptual path is:

```text
SQL domain data → MESH Gateway → validation through IAEI → AI
```
